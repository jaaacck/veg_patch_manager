# Pinned to tested minor ranges for reproducible-ish builds.
# For exact reproducibility, generate a lockfile in the container:
#   docker compose exec web pip freeze > requirements.lock
Django>=5.2,<5.3
djangorestframework>=3.15,<3.17
psycopg2-binary>=2.9,<2.10
gunicorn>=22,<24
Pillow>=10.4,<12
django-cors-headers>=4.4,<5
whitenoise>=6.6,<7
